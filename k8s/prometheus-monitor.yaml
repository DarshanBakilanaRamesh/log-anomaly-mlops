apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hdfs-log-anomaly-api
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: hdfs-log-anomaly-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
