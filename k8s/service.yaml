apiVersion: v1
kind: Service
metadata:
  name: hdfs-log-anomaly-api
spec:
  selector:
    app: hdfs-log-anomaly-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
